#include "main.h"
/**
* rev_string - a function that reverses a string
* @s: string to reverse
*/
void rev_string(char *s)
{
	int j = 0;
	int reverse = 0;

	for (j = 0; s[j] != '\0'; j++)
	{
		reverse++;
	}
}
